Use the `nimble-scaffold` agent to scaffold a new Svelte sheet.

Arguments: $ARGUMENTS

Pass all arguments directly to the agent. If arguments are missing (SheetName, DocumentType, DocumentSubtype), the agent will ask.
