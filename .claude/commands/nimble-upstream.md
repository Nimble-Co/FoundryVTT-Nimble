Use the `nimble-upstream` agent to compare the fork against upstream/dev and draft patches.

Arguments: $ARGUMENTS

The agent will fetch upstream, categorize all diverged changes, check for conflicts with fork-specific features, and draft patches — without performing any merges or destructive git operations.
