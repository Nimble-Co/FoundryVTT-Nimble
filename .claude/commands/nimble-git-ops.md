Use the `nimble-git-ops` agent to manage Git operations for the FoundryVTT-Nimble project.

Arguments: $ARGUMENTS

Pass all arguments directly to the agent. The agent handles branch creation/checkout, staging and committing related file groups, running pre-commit checks, pushing to origin, and managing pull requests, issues, and PR comments.
