## Progress

### Completed

- [x] CORE: Docker sandbox for afk-ralph.sh
  - Docker v27.2.0 confirmed available
  - Created `Dockerfile.sandbox`: node:22-slim image with git, pnpm, claude CLI
  - Created `~/.docker/cli-plugins/docker-sandbox` Docker CLI plugin (makes `docker sandbox run/build` work)
  - Plugin auto-builds the image on first `run` if not present; mounts project dir, ~/.claude, and ~/.gitconfig into the container
  - `afk-ralph.sh` (which uses `docker sandbox run claude ...`) now has all infrastructure it needs
  - Commit: CHORE: Add Dockerfile.sandbox and docker-sandbox CLI plugin for afk-ralph

- [x] CORE: Add `pnpm test` smoke-test for document proxy classes
  - Added `src/documents/documentProxy.test.ts` with 17 tests covering `ActorProxy` and `ItemProxy`
  - Verifies both proxy modules load without error (import succeeds)
  - Verifies `CONFIG.NIMBLE.Actor.documentClasses` has all 5 actor subclasses (base, character, npc, soloMonster, minion)
  - Verifies `CONFIG.NIMBLE.Item.documentClasses` has all 10 item subclasses (base, background, boon, class, feature, monsterFeature, object, ancestry, spell, subclass)
  - All 495 tests pass
  - Commit: TEST(core): Add smoke test verifying document proxy classes load without error


- [x] BUG: Chat cards not appearing when sidebar is collapsed
  - Root cause: `renderChatMessageHTML` hook stored Svelte component on `message._svelteComponent`.
    When FoundryVTT re-rendered the same message for the collapsed-sidebar notification overlay,
    the hook unmounted the component from the main chat log element before mounting to the notification.
    On sidebar expand, users saw blank (un-Sveltified) chat cards.
  - Fix: track component on the target DOM element (`target._nimbleSvelteComponent`) instead of
    on the message, so each rendered copy manages its own lifecycle independently.
  - File: `src/hooks/renderChatMessage.ts`
  - Commit: FIX: Chat cards not appearing when sidebar is collapsed
