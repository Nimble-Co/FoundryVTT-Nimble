/**
 * Type interfaces for rules to break circular dependencies
 * These are pure type definitions with no imports from documents
 */

// Empty for now - rules can be defined here if needed
export {};
